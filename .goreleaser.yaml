# GoReleaser configuration for the pmx CLI.
# Docs: https://goreleaser.com
#
# Release targets (per project requirements):
#   - darwin/arm64   (Apple Silicon)
#   - linux/amd64    (x86-64)
#   - linux/arm64    (aarch64)
#   - freebsd/amd64  (x86-64)
#   - freebsd/arm64  (aarch64)
#   - windows/amd64  (64-bit)
#
# Local snapshot:  goreleaser release --snapshot --clean
# Local dry-run:   goreleaser build --snapshot --clean
# Published by:    .github/workflows/release.yml on a v* tag.
version: 2

project_name: pmx

before:
  hooks:
    - go mod tidy
    - go mod download
    - mkdir -p ./manpages ./manpages-gz/man1 ./manpages-gz/man5 ./completions ./completions/.bin
    - go run ./cmd/docgen -out ./manpages -version {{ .Version }} -date {{ .CommitDate }}
    # Compressed copies for the deb/rpm packages only (lintian wants packaged
    # man pages gzipped); archives, the cask, and `make man` keep the
    # uncompressed ./manpages tree untouched. -n omits name/mtime from the
    # gzip header so the packages stay byte-identical across rebuilds.
    - sh -c "for f in ./manpages/man1/*.1; do gzip -9 -n -c \"$f\" > \"./manpages-gz/man1/$(basename \"$f\").gz\"; done"
    - sh -c "for f in ./manpages/man5/*.5; do gzip -9 -n -c \"$f\" > \"./manpages-gz/man5/$(basename \"$f\").gz\"; done"
    # `go run` can't control argv[0], and the command tree pmx/pve/pbs/pdm
    # expose is picked from argv[0] (see internal/cli.Persona), so build once
    # and invoke through persona-named links to generate each one's script.
    - sh -c "go build -o ./completions/.bin/pmx ./cmd/pmx && for p in pve pbs pdm; do ln -sf pmx ./completions/.bin/$p; done"
    - sh -c "for p in pmx pve pbs pdm; do ./completions/.bin/$p completion bash > ./completions/$p.bash; done"
    - sh -c "for p in pmx pve pbs pdm; do ./completions/.bin/$p completion zsh  > ./completions/_$p; done"
    - sh -c "for p in pmx pve pbs pdm; do ./completions/.bin/$p completion fish > ./completions/$p.fish; done"

builds:
  - id: pmx
    main: ./cmd/pmx
    binary: pmx
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    # Reproducible builds: pin the embedded timestamp to the commit.
    mod_timestamp: "{{ .CommitTimestamp }}"
    ldflags:
      - -s -w
      - -X github.com/fivetwenty-io/proxmox-cli/internal/version.Version={{ .Version }}
      - -X github.com/fivetwenty-io/proxmox-cli/internal/version.Commit={{ .ShortCommit }}
      - -X github.com/fivetwenty-io/proxmox-cli/internal/version.Date={{ .CommitDate }}
    goos:
      - linux
      - darwin
      - freebsd
      - windows
    goarch:
      - amd64
      - arm64
    # Trim the build matrix to exactly the requested targets.
    ignore:
      - goos: darwin
        goarch: amd64
      - goos: windows
        goarch: arm64

archives:
  - id: pmx
    ids:
      - pmx
    # tar.gz everywhere except Windows, which gets a .zip.
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    # e.g. pmx_1.0.0_darwin_arm64.tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - README.md
      - LICENSE
      - src: manpages/man1/*.1
        dst: share/man/man1
      - src: manpages/man5/*.5
        dst: share/man/man5
      # NB: these three need the full destination *file* path, not just the
      # directory — a literal (non-glob) src with a directory-only dst drops
      # the filename (goreleaser resolves dst="." relative to the src file
      # itself for non-glob sources), collapsing the file to e.g.
      # share/bash-completion/completions with no pmx.bash suffix. Verified
      # empirically via `tar tzf`; see task-8-report.md.
      - src: completions/pmx.bash
        dst: share/bash-completion/completions/pmx.bash
      - src: completions/_pmx
        dst: share/zsh/site-functions/_pmx
      - src: completions/pmx.fish
        dst: share/fish/vendor_completions.d/pmx.fish
      - src: completions/pve.bash
        dst: share/bash-completion/completions/pve.bash
      - src: completions/_pve
        dst: share/zsh/site-functions/_pve
      - src: completions/pve.fish
        dst: share/fish/vendor_completions.d/pve.fish
      - src: completions/pbs.bash
        dst: share/bash-completion/completions/pbs.bash
      - src: completions/_pbs
        dst: share/zsh/site-functions/_pbs
      - src: completions/pbs.fish
        dst: share/fish/vendor_completions.d/pbs.fish
      - src: completions/pdm.bash
        dst: share/bash-completion/completions/pdm.bash
      - src: completions/_pdm
        dst: share/zsh/site-functions/_pdm
      - src: completions/pdm.fish
        dst: share/fish/vendor_completions.d/pdm.fish

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_SHA256SUMS"
  algorithm: sha256

nfpms:
  - id: pmx
    package_name: pmx
    ids:
      - pmx
    vendor: FiveTwenty Inc.
    homepage: https://github.com/fivetwenty-io/proxmox-cli
    maintainer: Wayne E. Seguin <wayneeseguin@fivetwenty.io>
    description: Proxmox command-line interface (VE / PBS / PDM).
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/bin
    contents:
      - src: /usr/bin/pmx
        dst: /usr/bin/pve
        type: symlink
      - src: /usr/bin/pmx
        dst: /usr/bin/pbs
        type: symlink
      - src: /usr/bin/pmx
        dst: /usr/bin/pdm
        type: symlink
      # lintian requires packaged man pages to be gzipped; these come from the
      # manpages-gz before-hook, not the uncompressed ./manpages tree used by
      # the archives and the cask.
      - src: ./manpages-gz/man1/*.1.gz
        dst: /usr/share/man/man1/
      - src: ./manpages-gz/man5/*.5.gz
        dst: /usr/share/man/man5/
      - src: ./completions/pmx.bash
        dst: /usr/share/bash-completion/completions/pmx
      - src: ./completions/_pmx
        dst: /usr/share/zsh/site-functions/_pmx
      - src: ./completions/pmx.fish
        dst: /usr/share/fish/vendor_completions.d/pmx.fish
      - src: ./completions/pve.bash
        dst: /usr/share/bash-completion/completions/pve
      - src: ./completions/_pve
        dst: /usr/share/zsh/site-functions/_pve
      - src: ./completions/pve.fish
        dst: /usr/share/fish/vendor_completions.d/pve.fish
      - src: ./completions/pbs.bash
        dst: /usr/share/bash-completion/completions/pbs
      - src: ./completions/_pbs
        dst: /usr/share/zsh/site-functions/_pbs
      - src: ./completions/pbs.fish
        dst: /usr/share/fish/vendor_completions.d/pbs.fish
      - src: ./completions/pdm.bash
        dst: /usr/share/bash-completion/completions/pdm
      - src: ./completions/_pdm
        dst: /usr/share/zsh/site-functions/_pdm
      - src: ./completions/pdm.fish
        dst: /usr/share/fish/vendor_completions.d/pdm.fish
      - src: LICENSE
        dst: /usr/share/doc/pmx/LICENSE

# NOTE: `brews:` is hard-deprecated as of the installed goreleaser (v2.17,
# deprecated since v2.10/hard since v2.16) — `goreleaser check` fails on it.
# `homebrew_casks:` is the maintained replacement; see
# https://goreleaser.com/deprecations#brews. Casks don't have a Formula-style
# `install` script, so the pve/pbs/pdm persona aliases are created/removed via
# postflight/uninstall hooks instead of `bin.install_symlink`.
homebrew_casks:
  - name: pmx
    ids:
      - pmx
    repository:
      owner: fivetwenty-io
      name: homebrew-tap
      # envOrDefault (not `.Env.HOMEBREW_TAP_GITHUB_TOKEN` directly) so
      # `goreleaser check`/local snapshots don't fail when the tap secret is
      # unset — direct map access errors under missingkey=error.
      token: '{{ envOrDefault "HOMEBREW_TAP_GITHUB_TOKEN" "" }}'
    directory: Casks
    homepage: https://github.com/fivetwenty-io/proxmox-cli
    description: Proxmox command-line interface (VE / PBS / PDM)
    # Skip pushing the cask whenever the tap token is not configured, so a
    # release without the HOMEBREW_TAP_GITHUB_TOKEN secret still succeeds
    # (`index .Env` is nil-safe under missingkey=error, unlike direct access).
    skip_upload: '{{ if index .Env "HOMEBREW_TAP_GITHUB_TOKEN" }}false{{ else }}true{{ end }}'
    binaries:
      - pmx
    manpages:
      - share/man/man1/*.1
      - share/man/man5/*.5
    # goreleaser's cask `completions:` stanza takes exactly one file per
    # shell (a single path, not a list), so the pve/pbs/pdm persona
    # completions generated for the archives/deb/rpm cannot be attached
    # here too — the cask ships pmx completions only. The persona binaries
    # themselves are still installed (see the postflight hook below), just
    # without their own shell completions via Homebrew.
    completions:
      bash: share/bash-completion/completions/pmx.bash
      zsh: share/zsh/site-functions/_pmx
      fish: share/fish/vendor_completions.d/pmx.fish
    hooks:
      post:
        install: |
          %w[pve pbs pdm].each do |persona|
            link = HOMEBREW_PREFIX/"bin"/persona
            link.delete if link.symlink? || link.exist?
            link.make_symlink("pmx")
          end
        uninstall: |
          %w[pve pbs pdm].each do |persona|
            link = HOMEBREW_PREFIX/"bin"/persona
            link.delete if link.symlink?
          end

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot-{{ .ShortCommit }}"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "merge conflict"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: fivetwenty-io
    name: proxmox-cli
  draft: false
  prerelease: auto
  name_template: "pmx {{ .Version }}"
  header: |
    ## pmx {{ .Version }}

    Proxmox command-line interface.

    ### Install

    Download the archive for your platform from the assets below, verify it
    against `pmx_{{ .Version }}_SHA256SUMS`, extract, and place `pmx` on your `PATH`.

    ### Persona symlinks (optional)

    `pmx` inspects how it was invoked (`argv[0]`) to decide which commands to
    expose: named (or symlinked as) `pve`, it hoists the Proxmox VE groups to
    the root; named `pbs`, it hoists the Proxmox Backup Server groups; named
    `pdm`, it hoists the Proxmox Datacenter Manager groups. The archive ships
    a single `pmx` binary — most archive formats (notably `.zip`, used for
    the Windows build) do not reliably preserve symlinks, so create them
    yourself after installing:

    ```bash
    sudo ln -s pmx /usr/local/bin/pve
    sudo ln -s pmx /usr/local/bin/pbs
    sudo ln -s pmx /usr/local/bin/pdm
    ```

    (`make install`, when building from source, creates all three symlinks
    automatically.) See the [README's Personas section](https://github.com/fivetwenty-io/proxmox-cli#personas)
    for the full command surface each persona exposes.
