# Compiled binaries
bin/
# The built `pmx` binary at the repo root only — anchored so it does not match
# the cmd/pmx/ source directory.
/pmx
*.test

# Distribution
dist/

# Coverage
coverage.out
coverage.html

# Build artifacts
*.o
*.a
*.so

/manpages/
/manpages-gz/
/completions/

# Local lab/stack configuration (holds addresses and context names)
/config/stack.toml
/config/.env.test
