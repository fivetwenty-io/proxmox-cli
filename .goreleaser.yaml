# GoReleaser configuration for the pve CLI.
# Docs: https://goreleaser.com
#
# Release targets (per project requirements):
#   - darwin/arm64   (Apple Silicon)
#   - linux/amd64    (x86-64)
#   - linux/arm64    (aarch64)
#   - windows/amd64  (64-bit)
#
# Local snapshot:  goreleaser release --snapshot --clean
# Local dry-run:   goreleaser build --snapshot --clean
# Published by:    .github/workflows/release.yml on a v* tag.
version: 2

project_name: pve

before:
  hooks:
    - go mod tidy
    - go mod download

builds:
  - id: pve
    main: ./cmd/pve
    binary: pve
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    # Reproducible builds: pin the embedded timestamp to the commit.
    mod_timestamp: "{{ .CommitTimestamp }}"
    ldflags:
      - -s -w
      - -X github.com/fivetwenty-io/pve-cli/internal/version.Version={{ .Version }}
      - -X github.com/fivetwenty-io/pve-cli/internal/version.Commit={{ .ShortCommit }}
      - -X github.com/fivetwenty-io/pve-cli/internal/version.Date={{ .CommitDate }}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    # Trim the build matrix to exactly the requested targets.
    ignore:
      - goos: darwin
        goarch: amd64
      - goos: windows
        goarch: arm64

archives:
  - id: pve
    ids:
      - pve
    # tar.gz everywhere except Windows, which gets a .zip.
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    # e.g. pve_1.0.0_darwin_arm64.tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - README.md

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_SHA256SUMS"
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot-{{ .ShortCommit }}"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "merge conflict"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: fivetwenty-io
    name: pve-cli
  draft: false
  prerelease: auto
  name_template: "pve {{ .Version }}"
  header: |
    ## pve {{ .Version }}

    Proxmox VE command-line interface.

    ### Install

    Download the archive for your platform from the assets below, verify it
    against `pve_{{ .Version }}_SHA256SUMS`, extract, and place `pve` on your `PATH`.
