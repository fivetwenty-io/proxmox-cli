// Package initcmd implements the `pmx init` command group, which scaffolds
// local configuration. Its sole sub-command, `pmx init config`, writes a
// commented config.yml template the user fills in.
package initcmd
