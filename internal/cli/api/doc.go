// Package api implements the pmx `api` raw HTTP passthrough command group
// (GET/POST/PUT/DELETE against the active context) and the canonical `auth`
// command (login/logout/status/whoami/refresh/set-token/set-password).
package api
