// Package initcmd implements the `pve init` command group, which scaffolds
// local configuration. Its sole sub-command, `pve init config`, writes a
// commented config.yml template the user fills in.
package initcmd
